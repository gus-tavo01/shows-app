describe('shows-service getSomething', () => {
  test('When is valid, expect true', async () => {
    // Arrange
    const expectedResult = 'One';

    // Assert
    expect('One').toBe(expectedResult);
  });
});
